query productMetafields($id: ID!, $keys: [String!]) {
  node(id: $id) {
    ... on Product {
      metafields(first: 250, keys: $keys) {
        nodes {
          namespace
          key
          value
          type
        }
      }
    }
  }
}
